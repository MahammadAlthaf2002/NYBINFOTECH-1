import {useNavigate} from 'react-router-dom'; import {useState} from 'react'; import {allProducts} from '../../data/catalog'; import Icon from '../ui/Icon';
export default function Recommended(){const nav=useNavigate(); const [likes,setLikes]=useState({}); return <section className="section"><div className="sectionHead"><div><span className="eyebrow">CURATED FOR YOU</span><h2>Recommended today</h2></div><button className="textBtn" onClick={()=>nav('/service/shopping')}>Shop all <Icon name="arrow" size={15}/></button></div><div className="productRow">{allProducts.slice(0,4).map(p=><article className="productCard" key={p.id} onClick={()=>nav(`/product/${p.id}`)}><div className="productImg"><img src={p.image}/><button aria-label="Save item" onClick={e=>{e.stopPropagation();setLikes(v=>({...v,[p.id]:!v[p.id]}))}}><Icon name="heart" size={17} className={likes[p.id]?'liked':''}/></button><label>{p.tag}</label></div><div className="productInfo"><b>{p.name}</b><div><strong>₹{p.price.toLocaleString('en-IN')}</strong><del>₹{p.oldPrice.toLocaleString('en-IN')}</del></div><span>★ {p.rating} · Free delivery</span></div></article>)}</div></section>}
