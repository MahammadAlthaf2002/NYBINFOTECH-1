import {Link,useNavigate} from 'react-router-dom'; import {useCart} from '../../context/CartContext'; import Icon from '../ui/Icon';
export default function RightSidebar(){const {items,total}=useCart();const nav=useNavigate();return <aside className="rightSidebar"><section className="memberCard"><div className="memberTop"><span className="memberAvatar">A</span><div><strong>NYB Member</strong><small>Account dashboard</small></div><button className="crown" onClick={()=>nav('/service/premium')}><Icon name="spark" size={18}/></button></div></section><section className="cartMini"><div className="sectionTitle"><strong>My Cart</strong><span>({items.length} items)</span><Link to="/cart">View cart</Link></div>{items.slice(0,4).map(item=><div className="miniItem" key={item.id}><img src={item.image} alt=""/><div><strong>{item.name}</strong><span>₹{item.price.toLocaleString('en-IN')}</span></div><small>Qty {item.quantity}</small></div>)}<div className="miniTotal"><span>Total</span><strong>₹{total.toLocaleString('en-IN')}</strong></div><Link className="checkoutMini" to="/checkout">Proceed to checkout <Icon name="arrow" size={14}/></Link></section></aside>}
