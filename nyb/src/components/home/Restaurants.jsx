import {useNavigate} from 'react-router-dom'; import {products} from '../../data/catalog';
export default function Restaurants(){const nav=useNavigate(); return <section className="section"><div className="sectionHead"><div><span className="eyebrow">FOOD NEAR YOU</span><h2>Popular right now</h2></div><button className="textBtn" onClick={()=>nav('/service/food')}>See restaurants →</button></div><div className="restaurantRow">{products.filter(p=>p.category==='food').map(p=><article className="restaurantCard" key={p.id} onClick={()=>nav(`/product/${p.id}`)}><img src={p.image}/><div><b>{p.name}</b><span>4.7 ★ · 25–35 min</span><strong>From ₹{p.price}</strong></div></article>)}<article className="restaurantCard moreCard" onClick={()=>nav('/service/food')}><div className="moreCircle"><span>+</span></div><b>Explore more</b><span>Nearby restaurants</span></article></div></section>}
