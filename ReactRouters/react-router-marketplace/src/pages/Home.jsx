import { Link } from "react-router-dom";
import { products } from "../data/products";
import ProductCard from "../components/ProductCard";
export default function Home(){return <><section className="hero"><div><p className="eyebrow">REACT ROUTER PRACTICE</p><h1>One project.<br/><span>Every route tells a story.</span></h1><p>Practice static routes, dynamic product URLs, navigation links, nested routes, protected pages and a 404 fallback.</p><div className="hero-actions"><Link className="btn" to="/products">Explore Products</Link><Link className="btn secondary" to="/about">How it Works</Link></div></div><div className="route-map">/ <span>home</span><br/>/products <span>static</span><br/>/products/:id <span>dynamic</span><br/>/dashboard <span>protected</span><br/>/dashboard/profile <span>nested</span></div></section><section className="section-head"><div><p className="eyebrow">STATIC + DYNAMIC</p><h2>Featured Products</h2></div><Link to="/products">See all →</Link></section><div className="grid">{products.slice(0,3).map(p=><ProductCard key={p.id} product={p}/>)}</div></>}