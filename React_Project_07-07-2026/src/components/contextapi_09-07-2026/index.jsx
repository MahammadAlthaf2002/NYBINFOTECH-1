import React from "react";
import ReactDOM from "react-dom/client";
import App from "../Component_09-07-2026/App";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);