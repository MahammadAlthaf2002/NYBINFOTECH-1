import { NavLink, Outlet, useNavigate } from "react-router-dom";
export default function Dashboard(){const navigate=useNavigate(); function logout(){localStorage.removeItem("routehubLoggedIn");navigate("/login");} const cls=({isActive})=>isActive?"side-link selected":"side-link"; return <section className="dashboard"><aside><p className="eyebrow">PROTECTED AREA</p><h2>Dashboard</h2><NavLink end to="/dashboard" className={cls}>Overview</NavLink><NavLink to="profile" className={cls}>Profile</NavLink><NavLink to="settings" className={cls}>Settings</NavLink><button onClick={logout}>Logout</button></aside><main className="dashboard-content"><Outlet/></main></section>}ace