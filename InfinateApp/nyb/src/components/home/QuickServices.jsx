import {useNavigate} from 'react-router-dom'; import Icon from '../ui/Icon';
const quick=[['bills','Bill payments','Pay electricity, water & more','wallet'],['money','Send money','Secure transfers','send'],['recharge','Mobile recharge','Plans & offers','recharge'],['insurance','Insurance','Protect what matters','insurance'],['gold','Gold investment','Start from ₹10','spark']];
export default function QuickServices(){const nav=useNavigate();return <section className="section"><div className="sectionHead"><div><span className="eyebrow">QUICK ACTIONS</span><h2>Get things done faster</h2></div></div><div className="quickGrid">{quick.map(([slug,title,sub,icon])=><button key={slug} onClick={()=>nav(`/service/${slug}`)}><div><Icon name={icon}/></div><span><b>{title}</b><small>{sub}</small></span><Icon name="arrow" size={16}/></button>)}</div></section>}
