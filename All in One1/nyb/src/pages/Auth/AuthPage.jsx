import {useState} from 'react'; import {useNavigate,useParams} from 'react-router-dom'; import Icon from '../../components/ui/Icon'; import {images} from '../../data/catalog';
export default function AuthPage(){const {mode='login'}=useParams(); const isLogin=mode==='login'; const nav=useNavigate(); const [form,setForm]=useState({name:'',email:'',password:''}); const [error,setError]=useState(''); const submit=e=>{e.preventDefault(); if(!form.email||!form.password||(!isLogin&&!form.name)){setError('Please complete all required fields.');return} const user={name:form.name||form.email.split('@')[0],email:form.email}; localStorage.setItem('nyb_user',JSON.stringify(user)); nav('/account');}; return <div className="authPage"><div className="authVisual"><img src={images.phone}/><div><span className="eyebrow">NYB INFOTECH</span><h1>One app.<br/><strong>Infinite possibilities.</strong></h1><p>Shop, order, pay and manage everyday services with one secure account.</p></div></div><div className="authCard"><button className="authBrand" onClick={()=>nav('/')}><span className="brandMark">N</span><b>NYB Infotech</b></button><span className="eyebrow">{isLogin?'WELCOME BACK':'GET STARTED'}</span><h2>{isLogin?'Sign in to your account':'Create your account'}</h2><p>{isLogin?'Continue where you left off.':'Set up your NYB account in less than a minute.'}</p><form onSubmit={submit}>{!isLogin&&<label>Full name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Your name"/></label>}<label>Email address<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com"/></label><label>Password<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="••••••••"/></label>{error&&<div className="formError">{error}</div>}<button className="primary full">{isLogin?'Sign in':'Create account'} <Icon name="arrow" size={16}/></button></form><div className="authSwitch">{isLogin?'New to NYB?':'Already have an account?'} <button onClick={()=>nav(`/auth/${isLogin?'register':'login'}`)}>{isLogin?'Create account':'Sign in'}</button></div><small className="authNote">By continuing, you agree to NYB's terms and privacy policy.</small></div></div>}
