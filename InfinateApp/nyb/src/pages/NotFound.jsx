import {useNavigate} from 'react-router-dom'; export default function NotFound(){const n=useNavigate();return <div className="emptyState"><h1>Page not found</h1><p>That NYB page doesn't exist.</p><button className="primary" onClick={()=>n('/')}>Go home →</button></div>}
