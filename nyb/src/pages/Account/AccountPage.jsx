import {useNavigate} from 'react-router-dom'; import Icon from '../../components/ui/Icon';
export default function AccountPage(){const nav=useNavigate(); const user=JSON.parse(localStorage.getItem('nyb_user')||'null'); if(!user){nav('/auth/login');return null} const logout=()=>{localStorage.removeItem('nyb_user');nav('/')}; return <div className="accountPage"><div className="accountHead"><div className="avatar large">{user.name[0].toUpperCase()}</div><div><span className="eyebrow">MY ACCOUNT</span><h1>{user.name}</h1><p>{user.email} · Premium member</p></div><button className="outline" onClick={logout}><Icon name="logout" size={16}/>Sign out</button></div><div className="accountGrid">{[['Orders','12','shopping'],['Wishlist','8','heart'],['Rewards','250 pts','spark'],['Saved cards','2','wallet']].map(([a,b,i])=><div className="accountStat" key={a}><Icon name={i}/><span><b>{b}</b><small>{a}</small></span></div>)}</div><div className="accountPanel"><div><span className="eyebrow">PROFILE</span><h2>Account settings</h2><p>Your local account is ready. Add a real backend later for cloud sync and payments.</p></div><button className="primary" onClick={()=>nav('/cart')}>View my cart <Icon name="arrow" size={16}/></button></div></div>}
