import {useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {useCart} from '../../context/CartContext'; import Icon from '../../components/ui/Icon';
export default function Checkout(){const {total,clearCart}=useCart();const nav=useNavigate();const [done,setDone]=useState(false);const [form,setForm]=useState({name:'',phone:'',address:'',payment:'UPI'});const submit=e=>{e.preventDefault();setDone(true);clearCart()};if(done)return <div className="successPage"><div className="successIcon"><Icon name="check" size={34}/></div><span className="eyebrow">ORDER CONFIRMED</span><h1>You're all set.</h1><p>Your order request has been recorded successfully. A confirmation would be sent to your registered contact in a production backend.</p><button className="primary" onClick={()=>nav('/')}>Continue browsing <Icon name="arrow" size={16}/></button></div>;return <div className="checkoutPage"><div><button className="backLink" onClick={()=>nav(-1)}>← Back</button><span className="eyebrow">SECURE CHECKOUT</span><h1>Complete your order</h1><p>Enter delivery details and choose a payment method.</p><form className="checkoutForm" onSubmit={submit}><label>Full name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Your name"/></label><label>Mobile number<input required value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} placeholder="10-digit mobile number"/></label><label>Delivery address<textarea required value={form.address} onChange={e=>setForm({...form,address:e.target.value})} placeholder="House, street, city, PIN"/></label><label>Payment method<select value={form.payment} onChange={e=>setForm({...form,payment:e.target.value})}><option>UPI</option><option>Card</option><option>Cash on delivery</option></select></label><button className="primary full">Place order · ₹{total.toLocaleString('en-IN')} <Icon name="arrow" size={16}/></button></form></div><aside className="checkoutSide"><span>SECURE</span><h2>₹{total.toLocaleString('en-IN')}</h2><p>Encrypted checkout experience.</p><div><Icon name="check"/> No hidden convenience fee</div><div><Icon name="check"/> Order tracking available</div><div><Icon name="check"/> Support 24/7</div></aside></div>}
