import {useNavigate} from 'react-router-dom'; import Icon from '../ui/Icon'; import {categories} from '../../data/catalog';
export default function TopCategories(){const nav=useNavigate();return <section className="section"><div className="sectionHead"><div><span className="eyebrow">EXPLORE</span><h2>Popular categories</h2></div><button className="textBtn" onClick={()=>nav('/service/more')}>View all <Icon name="arrow" size={15}/></button></div><div className="categoryGrid">{categories.map(([icon,title,sub])=><button key={title} className="categoryCard" onClick={()=>nav(`/service/${icon==='more'?'more':icon}`)}><div className="catIcon"><Icon name={icon==='more'?'spark':icon} size={24}/></div><b>{title}</b><span>{sub}</span></button>)}</div></section>}
